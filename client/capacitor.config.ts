import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.bensadik.moroccam',
  appName: 'Moroccam',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
